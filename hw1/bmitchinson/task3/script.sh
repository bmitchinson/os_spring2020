#!/bin/bash

echo "****************"
echo "task3"
echo "****************"
files=0
let "files = "
# if [(($(ls -l | wc -l) - 1)) > "0"]
# then
#   echo "$(($(ls -l | wc -l) - 1))"
# fi
# echo "$((ls -l | grep -v '^total' | wc -l))"
echo "****************"